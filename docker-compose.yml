version: "3.9"
services:
  webportal:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
      - SECRET_KEY=supersecret_enterprise_key_change_me
    volumes:
      - project-web-home-antigravity/web-portal/data:/usr/src/app/data
    restart: unless-stopped
